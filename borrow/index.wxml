<view class="page">
  <view class="scan" bindtap="scanEnter">
    <image src="../images/qrcode.jpg" style="width:{{winWidth * 0.6}}px;height:{{winWidth * 0.6}}px; margin-left:{{winWidth * 0.2}}px; margin-top: {{(winHeight - 60 - winWidth * 0.6) * 0.5}}px;"></image>
  </view>
  <view class="cells btn_area" style="width: {{winWidth - 20}}px;">
    <button type="primary" size="default" bindtap="scanEnter"> 扫描图书二维码完成借阅 </button>
  </view>
</view>